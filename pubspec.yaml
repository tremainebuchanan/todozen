name: todozen
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  fl_chart: ^0.66.0
  flutter:
    sdk: flutter
  flutter_charts: ^0.5.2
  go_router: ^13.0.0
  google_fonts: ^6.1.0
  http: ^1.1.2
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


